{
  "$comment": "Standing loops for the project leader, shipped as DATA so they can be compared, not as prose that drifts. At every session start, resume and compaction the leader lists what is actually scheduled, compares this whole file against it by schedule AND by prompt, and re-creates anything missing or drifted. See .xezar/docs/leader-context-loading.md.",
  "version": 1,
  "compare": "whole-file",
  "loops": [
    {
      "id": "L1",
      "role": "unblock",
      "mechanism": "cron",
      "schedule": "*/10 * * * *",
      "mayDispatch": false,
      "prompt": "Check whether anything is waiting on you: a task in a blocked or question state, a review verdict you have not acted on, a PR that is mergeable now, or a finished task whose result you have not read. Act on what you find, one item at a time. Never start new work here - that is the pacing loop's job alone. If work finished and there is ready work to follow it, wake the pacing loop now instead of waiting for its clock; keep at most one such wake pending, replacing any earlier one. If nothing is waiting, do nothing and say so."
    },
    {
      "id": "L2",
      "role": "budget",
      "mechanism": "self-paced-wakeup",
      "schedule": "3600s",
      "mayDispatch": false,
      "prompt": "Walk the budget table. It is keyed by tool and login: move any metered **login** whose reset time has passed from `out` to `unknown`. Do not probe - the next real dispatch that prefers a lane on that login is the probe. A lane is a tool plus a model, and it is out only while EVERY login in its tool's rotation is out. If work is paused only because every preferred lane was out, and a login is now `unknown` or available, wake the pacing loop so it can resume that work - never start it yourself; keep at most one such wake pending, replacing any earlier one. Skip unlimited logins entirely. If nothing changed, do nothing and say so."
    },
    {
      "id": "L3",
      "role": "pace",
      "mechanism": "cron",
      "schedule": "*/30 * * * *",
      "mayDispatch": true,
      "prompt": "Decide whether to start more work. Count what is running: full gate runs, total tasks, tasks on the metered agent tool, and machine load. Start another task only while ALL of these hold: at most 2 gate runs, at most 10 tasks, at most 4 metered-tool tasks, machine load at or below 18. If there is ready work and headroom, pick the next item by least file overlap against every running task, using priority only to break a tie, then route that item through the routing table to choose its lane and model. If you are at a ceiling, queue and name which ceiling. If there is no ready work, do nothing and say so."
    }
  ],
  "ceilings": {
    "$comment": "Measured from real runs, not guessed. Attempt failure is about 20% at 1 concurrent gate run, 37% at 3, 90% at 4-5, and 100% at 6 or more.",
    "concurrentGateRuns": 2,
    "totalTasks": 10,
    "meteredToolTasks": 4,
    "machineLoad": 18
  },
  "rules": [
    "L3 is the only loop that may dispatch. L1 and L2 wake it instead of starting work themselves.",
    "At most one wake of L3 is pending at a time; re-waking replaces the pending one.",
    "A tick that changes nothing is a noop, and every prompt states its own no-op condition.",
    "A cron job expires after seven days, so the schedule and the prompt live here, not in the job.",
    "The owner asks for a loop; the leader only re-creates what this file already names. Never invent recurring work."
  ]
}
