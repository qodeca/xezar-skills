---
name: routing-json-plan
description: Owner-confirmed design (2026-09-22 grill) for replacing model-routing.md with a committed .xezar/routing.json + route script; next step is drafting defaults for owner review
metadata: 
  node_type: memory
  type: project
  originSessionId: 068bf781-56b3-4f20-b2af-2d1636b0a164
  modified: 2026-09-22T11:58:33.226Z
---

Grill interview closed 2026-09-22; owner confirmed the read-back. Replace the markdown routing table with JSON.

- File `.xezar/routing.json`; kit ships `kit/routing.json` + `kit/routing.schema.json`. No markdown routing file anywhere.
- 45 rows (not 37 workflows), each with a fixed string ID (rename = breaking), workflow, trigger, look-alikes, row bans; global bans + tie rule as top-level fields.
- Lanes listed once as `<tool>/<tool alias>` (e.g. `claude-code/opus`, never exact model IDs) with 7 tags: vendor, tier, vision, imageGeneration, local, enforcesToolLimits, advisoryOnly. Untagged lane rejected. Bans reference tags; a kit check rejects orders that break a ban.
- Login rotation per tool is INSIDE the committed file. Owner narrowed `references/rules.md:91`: chosen login IDs may be committed; emails/names/paths may not (check enforces; record in DECISIONS.md). Reserved leader login never in a rotation.
- Owner's defaults ship to everyone; every row ends in a Claude Code lane where bans allow. File keeps all lanes. Setup stops (at preflight, before the interview) if any row has no working lane on this machine — including image generation on a Claude-only machine (accepted cost). No optional rows.
- Script `route <id>` removes banned lanes, uninstalled tools (checked at run time), and logins absent from this machine's account list; leader then applies budget + never-the-author. `route --table` is the human view (no file). Leader reads triggers from the JSON itself to classify.
- One file edited in place per project; file records its defaults version; upgrades = three-way diff the owner applies via upgrade skill. Old markdown projects migrate by `--section routing` showing old vs new order.
- Kit owns the schema; frozen v1 from first release; listed in BACKWARD_COMPATIBILITY.md. Engine enforcement and config UI are out of scope now.
- Proof: first 10 live tasks, leader's lane == script's first available lane; setup passes on owner's machine and stops as expected on a Claude-only one.

Status 2026-09-22 (later): draft committed as `c091145` on `feature/routing-json` (not pushed). Since then (uncommitted): all lanes `enforcesToolLimits: false` per engine file:line answer – no runner blocks writes in read-only steps today. Owner chose: kit adds a bashAllowlist to every read-only step (Claude, pi then true); Codex lanes stay out of review/QA/security rows until qodeca/xezar#849 (0.19.0: signal = step allowedTools without Edit/Write; Codex read-only sandbox; Claude --disallowedTools) is released. Owner declined an MCP-off ask for Codex. Fable+Astra are `very-strong`, in `reservedLanes` (Astra only for generated-images, diagrams). Five-reviewer pass found: file must be a trust boundary with hard-coded security-row minimums in the route script; committed logins contested (identity leak, teammate overwrite, outranked rules.md:91); upgrade needs stored base defaults; frozen v1 with additionalProperties:false blocks growth; leader needs a `route --rows` projection; lost "wait when only DeepSeek" rules; two global bans stricter than owner's (different model vs vendor). Owner decided after the review (2026-09-22): logins STAY committed (rewrite references/rules.md:91 in the same PR); owner's local lanes (dgx-spark, mac-m4) SHIP in defaults (reconcile AGENTS.md "practice only its home project has"); v1 CAN GROW (readers ignore unknown keys, new fields optional – drop additionalProperties:false where needed); NO "wait when only DeepSeek" field. I (not owner) fix as drafting errors: global bans = different model (vendor only when one has budget); generated-images Astra only; trust boundary + route-script minimums for security rows; stored per-version defaults + kit-version string; `route --rows` projection.

Implementation (plan approved, `/Users/marcinobel/.claude/plans/greedy-spinning-dream.md`): engine requirements recorded as 0.19.0 issues – E1 #849 (PR #850, criterion G = allowlist matching; qa/acceptance/design-review option (a) pending engine owner), E2 #853, E3 #854, E4 #852, E5 #855, E6 #856 (pi honours bashAllowlist by prefix); arch-review role #851; #845 merged b10354f3. Codex read-only sandbox drops ALL network → a read-only Codex reviewer cannot `gh pr comment` (undecided). Owner: strict allowlists only on 5 readers (code-review, architecture-review, security-review, business-analysis, issue-triage); qa/acceptance/design-review = runs-code; pi off reader rows until #856. Part 1 built: kit/checks/git-read.sh, verdict-write.sh, catalog-check READER_BASH_PREFIXES / READ_ONLY_WORKFLOWS / RUNS_CODE_WORKFLOWS, allowlists on 5 workflows.

Part 1 committed as `2d472f8` (all 21 gates green, guard suite 76 cases). Engine #849 decided 2026-09-22 ("confined" design, not our option a): Codex read-only step = workspace-write sandbox, network ON, writable = worktree + evidence dir + handoff + TMPDIR; branch NOT protected; bashAllowlist IGNORED on Codex. Claude read-only = --disallowedTools Edit,Write,NotebookEdit, Bash kept (allowlist applies). Docs: ENFORCED (Claude) / CONFINED (Codex). So Codex reviewers can post comments; Codex lanes are confined, not enforcing. Engine leader will send #850 merged head + final doc wording; mark no lane enforcing before that.

Part 2 committed + pushed as `51598aa` (all 21 gates green, guard suite 88). Choices made: routing-rows.md NOT deleted yet – deleted in Part 3 with its readers (routing-interview.md, write.md, AGENTS.md). test-kit-catalog tolerates `[tool-limits]` errors only while compat.json floor < 0.19.0 → raising the floor forces lanes to be marked enforcing (do the floor raise after Verification 4). secondOpinion is judged against reserved + tool-limits but NOT the row's own `never` (owner put cheap DeepSeek as advisory on analysis). business-analysis row now writes:false. Security rows lost pi (release). Live Claude probe (engine PR #857, Claude CLI 2.1.278): Bash(<prefix>:*) needs a literal space after the prefix – no directory prefixes, no bare programs; compound commands split and each part must match. Our lists comply; asked engine whether a prefix matches when NOTHING follows it (worktree-setup.sh --readonly-init, gh repo view, gh label list) – awaiting their final probed lists to mirror. Fixed (uncommitted) security-review + issue-triage skills to type the full `bash .xezar/checks/worktree-setup.sh --readonly-init`. #850 MERGED into xezar main as fdbcbed4 (engine-only): read-only = resolved allowedTools has neither Edit nor Write ([] read-only, undefined writing). Claude ENFORCED (--disallowedTools Edit,Write,NotebookEdit; Bash(<prefix>:*) per allowlist entry). Codex CONFINED (workspace-write, network on, writable = worktree + evidence dir + handoff + TMPDIR; branch NOT protected; allowlist ignored; XEZ_CODEX_NETWORK=0 wins). pi/OpenCode unchanged. Still pending: kit lists PR #857 fix round, bare-command probe (run c13d5e74). Probe answers: bare command matches (Bash(x:*) == trailing space-star, matches bare) → our lists fine. Redirects and `mv` of $VAR paths DENIED → writes must be inside a granted script (verdict-write.sh is right). Env-var prefix before a command DENIED. Asked engine (pending): what feeds stdin – heredoc vs printf pipe (printf not granted?) – for verdict-write.sh packet and gh pr comment --body-file -. Stdin answer so far: only proven form is `jq -n '…' | bash .xezar/checks/<script>` with jq GRANTED in the engine's lists. Heredoc / printf cases queued in PR #857 fable re-check; result to come verbatim. If nothing better, add "jq" (reads only, no write flag) to our READER_BASH_PREFIXES + 5 workflows. Part 3 (uncommitted as of 2026-09-22): leader manual kit/docs/routing.md, guide/loops/docs, onboarding refs, routing-rows.md deleted, DECISIONS/BC/UPGRADE/AGENTS/README/coverage. Owner raised floor to 0.19.0 NOW (compat.json + pins) and decided: enforcing = claude/opus (engineModel "opus[1m]") + claude/sonnet; Codex and pi removed from all reading + security-and-release rows (note on each row). Tolerance in test-kit-catalog removed. state-names (E4 subcommand) still not done – waits for engine PR. CHANGELOG 3.0.0 at release. PR #42 open (draft). 5-agent review found: stdin feeder, security-scan.sh --out writes anywhere, escalation skips bans, cache reason newline forgery, raw gh too wide, readBase fallback trusts checkout, narrows one level only, silent drops, route guards untested. Owner chose: gh WRAPPER script; `jq -n` feeder (engine probe: printf/jq on left of a pipe need no entry; heredoc into a script DENIED, into gh OK). Owner still deciding: lock vs rules-only; asked for research on Qwen Code / JetBrains Junie as lockable reviewers. Engine answers (#849 comment "Engine answers for xezar-skills 3.0.0"): (1) project .claude/settings*.json permissions.allow RE-WIDENS Bash on Claude read-only steps (no --setting-sources) – only file tools are hard-removed; engine owner considering a fix; (2) allowedTools [] = Codex still has shell, pi gets ALL default tools – never use [] as "no tools"; (3) opus[1m] accepted by task_create; (4) accounts file global layout ~/.xezar/agent-accounts.json, single-project <project>/.xezar/…; (5) pi without agentProfile stays on pi accounts; (6) Codex handoff dir writable; (7) cd probe pending. #856 (pi per-command allowlist) IN 0.19.0 scope, implementation started. #857 merged (engine kit lists, b7e282f2). E5 #855 closed/verified. 0.19.0 date not set. Earlier OPEN owner question (now answered by the above) for task #20: does Codex "confined" count as enforcing for the tool-limits ban? If not, Codex leaves all reader + security rows and high-risk-other-vendor cannot be met for Claude-authored work.

Review fixes DONE (uncommitted, 2026-09-22; all 21 gates green, guard suite 103): gh-write.sh wrapper (never adds approval labels, never lifts blocking ones); reader lists drop raw gh comment/edit and security-scan.sh, add `jq -n` + gh-write.sh; READ_ONLY_WORKFLOWS keyed by file name, tools limited to Read/Grep/Glob/Bash; catalog-check refuses non-reading Bash rules in project .claude/settings*.json; TRUST_BOUNDARIES + loops.json, .xezar/docs|skills, .claude/settings; security-review 'confirmed, same vendor' interim wording (owner kept the lock). route.mjs: shared banReasons (escalation meets all bans), ENFORCING_RUNNERS={claude} cap, security class from workflows + full narrows chain, one-line sanitising, cache keeps only known lanes, readBase refuses without origin/HEAD (refs/remotes/origin/<b>), source= line on stdout, account file per engine layout (workspace.json → project, else $XEZ_HOME/~/.xezar), fable+haiku enforcing. Engine news: #858 state-names merged 84cc41ed (fixture packages/xezar/src/__fixtures__/state-names.json); pi per-command allowlist is #861 (in review), add "pi" to ENFORCING_RUNNERS + FACT 17 pin after refusal test; #862: Codex prefix_rule verified but NOT a lock (no default deny, user rules widen); viable Codex path = per-thread PreToolUse hook; engine proposes one shared lock module – owner's call. Next: local-tree.sh state-names, commit when asked.

Engine update 2 (2026-09-22): #859 merged 7147c938 – a verdict packet is accepted ONLY if the settling agent step declares `verdictRole: <role>` (4 roles: code-review, design-review, qa, architecture-review). 3.0.0 MUST add `verdictRole` to the kit's verdict steps (code-review, design-review, qa; architecture optional) AND add `verdictRole` to catalog-check STEP_KEYS, or packets are refused on 0.19.0. #849 probe: project .claude/settings.local.json re-widening CONFIRMED; engine #863 S1 adds `--setting-sources user` on read-only steps (kit settings guard stays as defence). `&&` compounds are DENIED on Claude even when both halves match (pipes still work). `opus[1m]` is in list_models. find -exec is pi-only, fixed in #861 (head a6ee722e, re-check running).

Committed + pushed 997c54c (2026-09-22): review fixes, verdictRole on code-review/design-review/qa, local-tree reads state-names, documented-output fixture bug fixed (ran leader loader without XEZAR_LEADER=1 – failed every onboarded gate since 2.1.0), CHANGELOG 3.0.0 (unreleased), package 3.0.0, 21 gates green, guards 107. 8cli dry run passed (scratch copy). Left for release day: engine 0.19.0 on npm, Claude refusal test, fresh onboarding, changelog date, un-draft, squash merge, tag v3.0.0, gh release. Assumed (not asked): proposed rows diagnose-bug/unit-tests NOT in 3.0.0 (additive later); paths.analysis stays reserved.

Owner 2026-09-22: ADD diagnose-bug + unit-tests rows (done, 47 rows) and DELETE paths.analysis (done, 3.0.0 break) – commit 50d32f3 pushed. Engine PR #866 (draft): `xezar lease gates --probe` → JSON {lease:{gates:true},slots:n}; adopt in repo-gates.sh once merged (current `nothing to run` match stays valid on 0.19.0).

Engine #865 merged 6a8ff114: engine's own kit has architecture-review.yaml with verdictRole: architecture-review + packet (APPROVE/REQUEST CHANGES). OUR kit's architecture review writes NO packet (comment only, CONFORMS/CONTRADICTS vocabulary) – compliant as is; owner chose KEEP AS IS (no architecture packet). Doc-update fixes committed + pushed 54ed2bf.

Engine #867 (agent quota: read_quota/check_quota, /api/v1/workspace/agent-quota) added to 0.19.0 by owner → 0.19.0 date moves (5 PRs + design gate). We are a named consumer (D7); routing-guidance changes (D27) must be agreed with us. I replied: first field = per-login status {runner, accountId, ok|out|unknown, resetsAt, checkedAt}. Fixture draft = engine PR #868; I confirmed shape and asked schemaVersion:1 + scope "agent-quota" + never ok at usedPercent>=100 – engine ACCEPTED both, owner approval pending, leader will say when #868 merges.

Engine #861 MERGED 3502665d (pi honours bashAllowlist per command) → task #27 (pi enforcing) needs only the live refusal test on 0.19.0. Engine finding `git fetch --upload-pack=<cmd>` runs a command: does NOT affect our kit – reading roles get no git, git-read.sh refuses `fetch` and any --upload-pack (verified live 2026-09-22). Also merged: #869 b081adac, #870 4ee9bf8b. Open: #866, #868, #863 S1 (shared lock module).

2026-09-22 later: #866 merged 862ec8fa → repo-gates.sh now uses `xezar lease gates --probe` (reads lease.gates===true) on engine>=0.19.0, keeps `nothing to run` for 0.17/0.18; FACT 15 pins both, guards 109; commit 7592977 pushed. #868 merged 7680dc0a (agent-quota fixture as agreed, SHA 967b5b4c…). Told the leader: routing names FINAL (.xezar/routing.json, routing.schema.json, route.mjs --check/--rows/--table/--file), floor sentence, and that kit docs/account-limits.md consumes #867 per-login status.

**Why:** owner finds markdown hard to work with and unclear for the leader; wants explicit, opinionated, UI-ready routing like `kit/loops.json`.
**How to apply:** next step is drafting `kit/routing.json` from `../xezar/.xezar/docs/model-routing.md` (read only; standing rules, drop dated quota notes) for owner review. Nothing else is built until they adjust it. Proof timing (before vs after release) is still open — ask after the review, since the format freezes at release. See [[onboarding-improvement-work]], [[onboarding-owner-decisions]].

2026-09-22 21:05: checked engine docs PR #873 (draft, head 61d2dd5a) against our kit – every name it states (.xezar/routing.json, .xezar/routing.schema.json, route.mjs --check/--rows/<row id>/--table/--file, source=unmerged, ok/unknown/out) matches the code. Engine still 0.18.0 on npm; 0.19.0 not released. PR #42 green + mergeable. Nothing more to do until 0.19.0 ships.

2026-09-22 ~22:00 Opus 5.5 re-rank (owner-approved, NOT committed yet): claude/opus tier strong→very-strong + lane note ("continue" on early end_turn); Opus first in the 13 rows sol led (sol second); security-review lanes [opus, fable] + reservedLanes.claude/fable.rows=["security-review"] (Anthropic re-routes cyber work on Opus 5.5 to Opus 4.8); FACT 17 pin updated to that exact value; CHANGELOG 3.0.0 paragraph. Defaults v1 edited in place (never shipped). All 21 gates green, guards 109.

2026-09-22 later: owner said REMOVE Fable completely from default routing (Opus 5.5 stronger/faster/cheaper). Done, uncommitted: claude/fable lane + reservation gone (10 lanes), security-review back to [opus] with note "ask the owner on re-route/refusal"; astra UNCHANGED (owner: keep images of all kinds + keep by-hand helper role in 4 rows). Tests moved from fable to astra (catalog escalation now checks analysis-specs-research; guard 787 uses astra); FACT 17 fable pin removed. 21 gates green, guards 109.

2026-09-22 ~23:00: owner: Astra = automatic security-review fallback ("release waits for 0.19.0 anyway"). Done, uncommitted: ENFORCING_RUNNERS={claude,codex} in route.mjs; astra enforcesToolLimits:true (other codex lanes stay false); security-review [opus, astra]; astra reserved rows + security-review; FACT 17 pins updated; guard 835 now uses pi lane; catalog escalation-ban test uses a no-limits copy. RELEASE BLOCKER: engine #885 (#863 S2 Codex PreToolUse lock) must be in 0.19.0 + our live Codex refusal test; if #885 slips, revert codex from ENFORCING_RUNNERS + astra out of security-review. Asked engine leader (uds 88735) which Codex project file can widen allow rules (to add a kit refusal like .claude settings guard). Agreed their #867 D27 + #849 F wording.
Later: added kit refusal of `.codex/rules/*.rules` prefix_rule unless decision prompt|forbidden (catalog-check.mjs), `.codex/` trust boundary (security-scan.mjs), tests + guard (110). BC/UPGRADE_NOTES/CHANGELOG updated. Uncommitted. Engine: 0.19.0 won't ship without #885 unless descoped; #886 in scope. OPEN: does an exec-policy allow rule skip the PreToolUse hook? If yes → Codex not enforcing (user-layer $CODEX_HOME/rules unseen by gates); engine leader will send the answer.
ANSWERED LIVE (#885 QA daf83920, Codex 0.155.1): an allow rule in project OR user `.codex/rules` does NOT skip the PreToolUse hook – hook still denied `touch`. Residual: a hook-ALLOWED command that also matches an allow rule runs outside the sandbox (our allowlist is reading cmds + kit scripts, so acceptable). #885 in fix round 1 (Q8 CODEX_HOME wrapper); engine owner approved writing hook+trust into the user's Codex profile with safeguards. Astra security fallback stays; still conditional on #885 merging + our live test.
2026-09-23: #885 HELD for engine owner's morning decision (2 fix rounds used; blockers: CODEX_HOME check breaks Codex writing runs, headless run hangs after denied turn). If no 3rd round / slips → revert Codex from ENFORCING_RUNNERS + astra out of security-review. Lock logic itself sound; Q9 unchanged.
2026-09-23: #885 APPROVED + QA-PASSED at 5b51b339 (Codex 0.156.0), merge chain running. Allow rule does NOT skip the hook WHEN the step has a bashAllowlist; WITHOUT one a user/project allow rule escapes the Codex sandbox – our catalog-check requires bashAllowlist on every reading step (security-review has one), so Astra fallback holds. Headless hang not reproduced. Engine leader now at uds:/tmp/cc-socks/53413.sock. Doc sweep done (coverage row 26, write.md, phase-record.md, security trigger .codex/, .codex trust-boundary pin+guard → 111 guards). All uncommitted.
2026-09-23: #885 MERGED 7d9db926. Resent engine leader (uds 53413) the verbatim D27 + #849 F texts I agreed; proposed NEW unconditional Codex half (hook only with bashAllowlist; without one an allow rule escapes sandbox) – not yet agreed.
2026-09-23: committed + pushed d470065 (Fable out, Astra security fallback, Codex rules refusal, doc sweep) on PR #42. #885 on main 7d9db926, CI green. Release still waits for 0.19.0 on npm + our live Claude/Codex refusal tests + fresh onboarding.
2026-09-23 step A: engine b16b86b1 checked (source build). GAP 1 (ours, owner: fix kit): shared pipe rule allows only argument-free 'bash <script>' on right → gh-write.sh/verdict-write.sh now accept ONE JSON request on stdin with no args; docs use jq -n (no printf); shared tail synced (36 copies); tests + 3 guards (114); proven with engine decideReadOnlyCommand (new allowed, old refused). Claude runtime matching of the no-arg form UNVERIFIED → release-day live test. GAP 2 = #903 (apply_patch) engine promises in final sha. Also fixed verify.md set_workspace_config shape and account-limits.md read_quota. Uncommitted. Step B (final sha) still to do.
2026-09-23: committed + pushed 81a5d91 (JSON-on-stdin write scripts, doc sweep) on PR #42. Waiting: engine #903 + final sha for step B, then 0.19.0 on npm → live tests (Claude no-arg pipe matching, Codex/pi refusal), fresh onboarding, release.
2026-09-23 LIVE Claude test PASSED on engine source 8cff8d43 (scratchpad/livetest, headless run, haiku): touch + redirect refused, new jq|bare-script allowed (old form also allowed on Claude – per-segment). Codex live BLOCKED: Codex credits depleted. Step B answered NOT YET. Engine owner moved release to 2026-09-24 (all #867 items), final sha will change. Quota shape: told engine prefer clean rename checkedAt→observedAt + loginKind, no alias (we read only status/resetsAt). How to run: cd project; XEZ_HOME=<scratch> node <src>/packages/xezar/dist/index.js run "<text>" --workflow <name> --single-project --no-import-global --output lines --no-open; step-level runner/model in YAML.
Live-test tip: headless run's LAST step is interactive; end the step prompt with a final line XEZ:DONE or it idles 15m (resources.idleTimeoutMinutes). My live test lacked it – expected, not a bug.
2026-09-23 LIVE Codex test PASSED at 8cff8d43 (luna, copied CODEX_HOME, XEZ_CODEX_BIN=~/.nvm/.../bin/codex; real CLI home is ~/.codex-cli): touch/old pipe/redirect/apply_patch refused, new form allowed; real profile untouched; copied auth.json deleted. Told engine: formal (a) on final sha after diff re-check. Remaining: fresh onboarding from npm; pi not in reviewer lanes.
2026-09-23: sent step B (a) CONFIRM for engine da87671b (conditional on main CI green); limits: fresh onboarding from npm, pi, version gate needs published 0.19.0. Our 3.0.0 release after fresh onboarding passes on npm package.
2026-09-23: engine 0.19.0 RELEASED (tag v0.19.0 → ccb5b2f6, npm latest 0.19.0, installed here). Waiting on owner's fresh onboarding run (bootstrap with Step 2 from local path /Users/marcinobel/Projects/xezar-skills – skills CLI can't read branch names with '/'). Then: changelog date, squash-merge PR #42, tag v3.0.0, gh release.
2026-09-23: **3.0.0 RELEASED** – PR #42 squash-merged 18464b8, branch deleted, tag v3.0.0, GitHub release published (Latest). Owner chose release BEFORE fresh onboarding run; onboarding (bootstrap, Step 2 from qodeca/xezar-skills now OK since main=3.0.0) still to do → a finding means 3.0.1. GitHub repo has none of the taxonomy labels though config labels.enabled=true (open). Engine leader promised onboarding-after-release not kept – tell them.
2026-09-23 8cli onboarding with 3.0.0 WORKED (28 min, 7 questions, PR #28 merged, 1 /mcp reconnect). 3.0.1 candidates (verified): (1) route.mjs:211 leader-login check spans ALL tools → codex built-in 'default' collides with leader claude/default (owner had to make ~/.codex-main); engine 409 'already this agent's default folder' missing from engine-refusals.md; (2) route.mjs:643 + changelog-fragments.mjs:403 main-guard fails through symlink → SILENT exit 0 (reproduced); (3) smoke-test.md tells agentProfile on step-list start – engine 0.19.0 refuses; (4) kit/xezar.gitignore misses workspace.json.bak; (5) bootstrap: remove local-scope xezar MCP after .mcp.json; engine message says 'xez' not 'xezar'.

## 3.0.1 released 2026-09-23 20:32Z (v3.0.1, 7a49146, PR #43)
Six onboarding fixes + GPT-6 swap folded in at owner's request: codex/gpt-5.6-sol→codex/gpt-6-sol, gpt-5.6-luna→gpt-6-luna in every row; 5.6 lanes kept defined but unused (owner choice); terra stays (no GPT-6 Terra); Claude aliases unchanged. defaults.version 2 + references/routing-defaults/2.json. Engine 0.19.0 passes model ids through (no allowlist). Pending: 8cli remediation per UPGRADE_NOTES (incl. --section routing for defaults v2); Sonnet 5.5 / Haiku 5.5 announced "coming weeks".
8cli remediation 2026-09-23: skills updated to 3.0.1; PR qodeca/8cli#30 (chore/xezar-3.0.1 → develop) = cp lines, gitignore, routing v2 three-way (kept claude rotation + qwen vision edit), codex rotation → default. Local-scope xezar MCP removed; ~/.codex-main (empty) deleted; codex-main stays in agent-accounts unused. Owner chose: leader reviews + owner merges #30.
